1、复习下函数指针
2、复习035法则，什么时候用0、3、5

3、c++中这些default和delete关键字是什么意思
        ~Test() = default;

        Test(const Test &) = delete;
        noexcept关键字是干嘛的

4、继承后面的访问修饰符的具体意思

5、补全035法则
拷贝构造函数、拷贝赋值运算符中的值类型，需要特殊处理吗？


User&& 两个&&是什么意思
左值右值复习


拷贝构造函数、拷贝赋值运算符 和 移动构造函数、移动复制运算符是通过左右值来区分的，为什么？移动语义std::move(u2);传入的也不是一个右值啊，这是一个左值把？

看下我的理解对不对，如果是普通资源，为了防止重复释放资源，要么通过拷贝构造&赋值语义，要么通移动构造&赋值语义来实现，但如果是文件句柄或者网络资源则建议使用禁止拷贝语义或者移动构造&赋值语义来实现。
对于普通资源（如动态内存）：拷贝语义和移动语义都可以使用，具体选择取决于需求：
	•	拷贝语义：需要创建独立副本。
	•	移动语义：需要高效转移资源所有权。
	•	对于文件句柄或网络资源（不可共享资源）：建议使用 禁止拷贝 和 支持移动语义 的方式来实现，确保资源唯一性并允许高效转移。